# Data directory

Place the educational churn dataset here as `churn.csv`.

Required target:

- `Exited`: 1 when the customer left and 0 otherwise.

The training script supports mixed numeric and categorical predictors and removes common identifier columns automatically. The dataset is not included because its redistribution rights must be verified separately.
